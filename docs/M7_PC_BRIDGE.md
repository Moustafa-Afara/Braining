# M7 — The PC agent bridge (Path B)

**Status:** specified 2026-09-05, not started. Earlier notes call this **M6**; the owner
reprioritised on 2026-09-05 and file generation took that number. Nothing about the design changed.

**Why it exists.** It is the original point of the whole project: *"لا أستطيع التحكم بالحاسب أو طلب
مهام من النموذج وأنا بعيد عن الحاسب."* Ollama over Tailscale gave the owner his PC's **model** from
anywhere. M7 gives him his PC's **hands** — running real tasks on the machine while away from it.

---

## 1. Blockers that were open since August — both now CLOSED, verified on the machine

`ANSWERS.md` Part 1 §4 deferred one question: *does the installed OpenCode have a headless mode?*
Checked directly on the owner's PC, 2026-09-05:

- **OpenCode 1.17.14**, at `C:\Users\ASUS\AppData\Roaming\npm\`. Its help lists
  **`opencode serve` — "starts a headless opencode server"**, plus `opencode run [message..]`
  (non-interactive), `opencode attach <url>`, `opencode agent`, `opencode export [sessionID]`.
  **The answer is yes.** OpenCode stays first choice; Claude Code is not needed as fallback.
- **Tailscale is already up on both devices**, which M7 assumed and never verified:
  - `100.117.245.110  ma` — the Windows PC
  - `100.68.141.70  redmi-note-13-pro-5g` — the phone, `active`
- **Node v22.20.0** and **Java 26** are installed, so the bridge has a runtime either way.

Nothing has to be installed to begin M7.

## 2. Shape

```
phone (Braining)  →  Tailscale  →  bridge  →  opencode serve  →  the PC's files
                                  (guards)     (localhost only)
```

**Why a bridge instead of talking to `opencode serve` directly.** The three mandatory guardrails
(`BRAINING.md` §7) are the entire safety story, and `opencode serve` does not enforce them. The
bridge is the only process on the Tailscale interface; OpenCode stays bound to `127.0.0.1` and is
reachable *only* through it. A design where the phone talks to OpenCode directly has no place to put
the approval gate, so it is not an option, however much simpler it looks.

**Language: Node.** Not a preference — `BRAINING.md` §8 says "pick the simplest to install", Node 22
is already there, and the bridge is a few hundred lines of HTTP. A Ktor server would drag a second
JVM build into a project whose Gradle setup is already the most fragile thing in it.

## 3. The three guardrails — `BRAINING.md` §7, non-negotiable

1. **Full change report.** Every file created, modified or deleted; every config or system setting
   touched; a complete log of what was done. Returned to the phone after *each* task and shown in
   the app. Implementation: snapshot the confined directory (path + size + mtime + hash) before and
   after, diff it, and pair that with OpenCode's own transcript. The report is generated by the
   **bridge**, not by the model — a model reporting on itself is not a report.
2. **Approval gate before any destructive action** — delete, bulk edit, or system command. The
   bridge classifies the *proposed* action, pauses, and pushes the question to the phone. Nothing
   destructive runs on a timeout or a default-yes.
3. **Working-directory confinement — widened to the whole disk on 2026-09-05, with a deny-list.**
   The owner answered "كل المجلدات حرفياً". `BRAINING.md` §7 allows exactly this ("unless explicitly
   widened"), so the grant is legitimate and is recorded in `ANSWERS.md` Part 20 §M7-1. **It is not
   executed bare.** A hard deny-list stays, read *and* write, and it exists to protect rulings the
   owner already made, not to overrule this one:
   - `braining-release.jks`, `keystore.properties*` — hard constraint 9. Losing or leaking the
     signing key means the app can never be updated on anyone's phone again.
   - `C:\Users\ASUS\.local\share\opencode\auth.json` — nine providers' API keys in clear text.
     An agent that can read it can leak them, which is hard constraint 3 (BYOK) broken from the
     inside.
   - `.ssh`, `.gnupg` and equivalent credential stores.

   **`C:\Windows` and `Program Files` were removed from the deny-list on 2026-09-05** at the
   owner's instruction — he wants the agent able to work with installed software. Note the practical
   limit rather than a policy one: writing to either still requires administrator rights, and the
   bridge is not to be run elevated, so writes there will simply fail unless that changes.

   Everything else is readable and writable. Path resolution still happens: every path is resolved to
   its real absolute form, and `..` and symlinks are evaluated **before** the deny-list is applied —
   a deny-list checked against the string the caller supplied is not a deny-list.

## 4. Security — the same standard as `LocalEndpoint`, and for the same reason

- The bridge binds to **the Tailscale address only** (`100.117.245.110`), never `0.0.0.0`, never
  `localhost`-plus-a-router-rule. `ANSWERS.md` Part 16 §P1 already closed the "just open a port"
  door for Ollama; an agent that *writes files* deserves more caution than one that answers
  questions, not less.
- **A shared token**, generated on the PC, entered once in the app and stored in the **Keystore**
  like any provider key. Tailscale already authenticates the device; the token means a second app on
  the same tailnet still cannot drive the PC.
- **`LocalEndpoint` is reused as-is** with `allowTunnel = true`. It already accepts exactly two
  shapes — a `.ts.net` MagicDNS name and `100.64/10` — and refuses everything routable. That guard
  is unit-tested and is a release blocker; M7 must not add a third shape.
- **An append-only audit log on the PC**, written by the bridge, that the app cannot edit.

## 5. App-side work, and two items that were deferred *into* this milestone

Re-read both against the code before building (`PROJECT_STATE.md` §10 entries 33 and 34 — twice
already a plan was carried out that the code had quietly made unnecessary):

- **The A/B classification call.** `BRAINING.md` §3 and `ANSWERS.md` Part 2 §11. It was deliberately
  not built in M4 because Path B did not exist, so a classifier would have spent a round trip to
  answer a question with one possible answer. `RoutingDecision.NeedsPc` **already exists**, so this
  adds a branch rather than widening a type.
- **The AI-router toggle** (`BRAINING.md` §5) — switches between the classifier and a rule table. It
  arrives with the classifier or not at all.
- A **PC-task screen**: what is running, the change report when it finishes, and the approval prompt
  when the gate fires. The approval prompt is the one piece of UI that must be impossible to miss.

## 6. Build order

1. Bridge skeleton: bind to the Tailscale address, token check, health endpoint. Prove the phone can
   reach it and an unauthenticated caller cannot.
2. Confinement + the before/after snapshot machinery. Test the escapes (`..`, symlink, absolute
   path) **before** wiring any agent in.
3. `opencode serve` behind it, one task type end to end, change report returned.
4. Approval gate.
5. App: NeedsPc branch, PC-task screen, token in Keystore.
6. Classifier + router toggle, last, once there is something real to route to.

## 7. The one test list

1. The bridge answers the phone over Tailscale and **refuses a request with no/!wrong token**.
2. It is **not reachable** from a non-tailnet network (phone on mobile data with Tailscale off).
3. A read-only task ("list what is in this folder") returns a correct result and a change report
   saying *nothing changed*.
4. A writing task creates a file; the change report names it exactly.
5. A destructive task **stops and asks**, and does nothing at all if refused.
6. A path escape (`../`, an absolute path outside the root, a symlink pointing out) is **refused**.
7. The audit log on the PC contains all of the above, in order.
8. Killing Tailscale mid-task fails safely — no half-applied change without a report.

## 8. The owner's decisions — all four answered 2026-09-05 (`ANSWERS.md` Part 20)

1. **Folder scope: the whole disk**, minus the deny-list in §3 above.
2. **Approval gate: destructive actions only**, as `BRAINING.md` §7 specifies.
3. **Keys: OpenCode's own — and they are already configured.** Verified on the machine:
   `C:\Users\ASUS\.local\share\opencode\auth.json` holds credentials for **nine** providers
   (anthropic, openai, google, deepseek, openrouter, huggingface, github-copilot, opencode,
   opencode-go), managed with `opencode auth`. **Nothing to fetch, and Braining never sees any of
   them** — the bridge starts OpenCode, and OpenCode reads its own file. A key that never passes
   through the app cannot leak from the app.
4. **Start: manual** — with one correction and one contradiction on record.
   - *Correction:* binding to the Tailscale address is **not** a local-network restriction.
     Tailscale reaches the PC from anywhere on earth; the phone is already on the tailnet and was
     seen active over a relay. The owner's requirement is satisfied by the design as written.
   - *Contradiction:* a bridge that is reachable while he is away must **already be running**.
     Manual start means that if he forgets before leaving, nothing is listening and nothing can
     start it remotely. Recorded as chosen, to be revisited the moment guardrails 1 and 2 are
     proven — auto-start is what actually delivers "ask for a task while away".

**Nothing to install.** OpenCode 1.17.14 with `serve`, Tailscale on both devices, Node 22 — all
already present.
